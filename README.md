separate branch of the main/rust port work focused on UI/UX improvements. Focus of most work is render/browser with limited engine exposure, though some changes are being worked through there too.
Will continue concurrent with the rust port. When the rust port is complete, this work will be staged into the main with the rust engine.
