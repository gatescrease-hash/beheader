[package]
name = "beheader-wasm"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "The browser binding behind the hosting and measurement proof."

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
beheader-engine = { workspace = true }
beheader-hostproof = { path = "../beheader-hostproof" }
wasm-bindgen = "0.2.128"
js-sys = "0.3.81"
